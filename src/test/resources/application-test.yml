spring:
  profiles:
    include: stdout

  jpa:
    hibernate:
      show-sql: true
      ddl-auto: create

server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0
